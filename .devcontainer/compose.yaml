services:
  dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    image: intake_html_table:dev
    # Overrides default command so things don't shut down after the process ends
    command: /bin/sh -c "while sleep 1000; do :; done"
    volumes:
      - ../:/intake-html-table
